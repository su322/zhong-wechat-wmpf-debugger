# Use 64-bit CPython 3.10+ on Windows.
# Python <= 3.8 cannot resolve pyfiglet==1.0.2, and unsupported/32-bit
# Python installs may try to build frida from source instead of using a wheel.
frida==16.1.11
psutil==5.9.7
pyfiglet==1.0.2
